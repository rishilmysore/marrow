# Plan: <slug>

<!-- marrow v0 — written for an executor with NO other context: fresh session, this file + AGENTS.md only.
     Trivial tasks don't get plans. Risky-novel work fills Spec before Tasks. -->

**Intent:** <!-- what this delivers and why, 1–2 sentences -->
**Class:** multi-file | risky-novel

## Success criteria (must be TRUE at close)

1. <!-- observable outcome, not a task: "search results carry provenance", "a session survives app restart" -->
2.

## Spec (risky-novel only — delete otherwise)

- Unknowns to resolve first:
- Approach chosen + alternatives rejected (one line why each):
- Failure modes / threats designed against (security-sensitive: name the attack surfaces and who abuses them):
- Rollback if it ships broken:

## Context

- Files / entry points:
- Binding constraints (from AGENTS.md / DECISIONS.md):

## Tasks

<!-- One atomic commit each. Record deviations inline as they happen. -->

1. [ ]
2. [ ]

## UI acceptance (UI tasks only — delete otherwise)

- [ ] Meets AGENTS.md Design tokens and bar (keyboard, responsive, both themes, empty/error states).
- [ ] <!-- task-specific visual criteria: hierarchy, spacing, motion -->

## Verify — the gate: cannot close while any row lacks evidence

| Check | How | Evidence |
|---|---|---|
| Fast gate | <!-- from AGENTS.md Verification --> | |
| Behavior | <!-- drive the real flow --> | |

## Budget

Stop if: <!-- bound, e.g. "3 failed attempts on the same failing test" or "task 2 grows past 5 files" -->
Tripped → record it here, add a STATE.md blocker, ask before continuing.

## Closeout

Run CLOSEOUT.md. Distilled line(s) destined for DECISIONS.md:

-
